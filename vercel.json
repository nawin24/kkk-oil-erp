{
  "framework": null,
  "installCommand": "echo 'No package manager install step needed for Flutter'",
  "buildCommand": "git clone https://github.com/flutter/flutter.git --depth 1 --branch 3.44.8 _flutter && _flutter/bin/flutter config --enable-web && _flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
